name: Audit Remediation Test Formatter

on:
  push:
    branches: [fix/governance-audit-m0-m2-remediation]

permissions:
  contents: write

concurrency:
  group: audit-remediation-test-formatter
  cancel-in-progress: true

jobs:
  format-and-self-remove:
    if: ${{ !contains(github.event.head_commit.message, 'format focused regression and remove bootstrap') }}
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
        with:
          ref: fix/governance-audit-m0-m2-remediation
          fetch-depth: 0
          persist-credentials: true
      - uses: pnpm/action-setup@v4
        with:
          version: 11.13.0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile --prefer-offline
      - name: Apply locked formatter
        run: |
          pnpm exec prettier --write \
            apps/desktop/renderer/src/audit-trash-reference-guard.ts \
            tests/integration/audit-m0-m2-remediation.test.ts
      - name: Remove bootstrap authorization
        shell: bash
        run: |
          node --input-type=module <<'NODE'
          import { readFile, writeFile } from 'node:fs/promises';
          const file = '.github/audit-remediations/m0-m2-2026-07-20.json';
          const manifest = JSON.parse(await readFile(file, 'utf8'));
          manifest.governancePaths = manifest.governancePaths.filter(
            (value) => value !== '.github/workflows/audit-format-test.yml',
          );
          await writeFile(file, `${JSON.stringify(manifest, null, 2)}\n`, 'utf8');
          NODE
          rm .github/workflows/audit-format-test.yml
      - name: Commit exact formatter output
        shell: bash
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m 'style(audit): format focused regression and remove bootstrap'
          git push origin HEAD:fix/governance-audit-m0-m2-remediation
