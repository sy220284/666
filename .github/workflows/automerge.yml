name: Auto Merge

on:
  workflow_run:
    workflows:
      - PR Policy
      - Task Governance
      - Quality
      - Security
      - Performance
      - Evidence
    types: [completed]

permissions:
  actions: write
  checks: read
  contents: write
  pull-requests: write

concurrency:
  group: automerge-main
  cancel-in-progress: false

jobs:
  automerge:
    if: ${{ github.event.workflow_run.event == 'pull_request' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          persist-credentials: false
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - name: Merge eligible pull request and schedule final main verification
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: node scripts/automerge.mjs
