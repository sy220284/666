name: Security

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review, converted_to_draft]

permissions:
  contents: read

concurrency:
  group: security-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  dependency-audit:
    if: ${{ github.event.pull_request.draft == false }}
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false
      - uses: pnpm/action-setup@v4
        with:
          version: 11.13.0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
      - name: Clean tree before dependency audit
        run: node .github/governance/assert-clean-tree.mjs
      - run: pnpm install --frozen-lockfile --prefer-offline
      - name: Audit high-severity dependency vulnerabilities
        run: pnpm audit --audit-level=high
      - name: Clean tree after dependency audit
        if: always()
        run: node .github/governance/assert-clean-tree.mjs

  secret-scan:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - name: Clean tree before tracked-file scan
        run: node .github/governance/assert-clean-tree.mjs
      - name: Scan tracked files
        run: node scripts/scan-secrets.mjs
      - name: Clean tree after tracked-file scan
        if: always()
        run: node .github/governance/assert-clean-tree.mjs

  application-security:
    if: ${{ github.event.pull_request.draft == false }}
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false
      - uses: pnpm/action-setup@v4
        with:
          version: 11.13.0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
      - name: Clean tree before application checks
        run: node .github/governance/assert-clean-tree.mjs
      - run: pnpm install --frozen-lockfile --prefer-offline
      - run: pnpm test:prepare
      - name: Run application security tests
        run: pnpm test:security
      - name: Clean tree after application checks
        if: always()
        run: node .github/governance/assert-clean-tree.mjs

  security:
    name: security
    if: always()
    needs:
      - dependency-audit
      - secret-scan
      - application-security
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    env:
      DRAFT_MODE: ${{ github.event.pull_request.draft }}
      AUDIT_RESULT: ${{ needs.dependency-audit.result }}
      SECRET_RESULT: ${{ needs.secret-scan.result }}
      APP_RESULT: ${{ needs.application-security.result }}
    steps:
      - name: Enforce security result
        shell: bash
        run: |
          set -euo pipefail
          printf 'draft=%s\naudit=%s\nscan=%s\napplication=%s\n' \
            "$DRAFT_MODE" "$AUDIT_RESULT" "$SECRET_RESULT" "$APP_RESULT"
          test "$SECRET_RESULT" = success
          if [ "$DRAFT_MODE" = true ]; then
            for result in "$AUDIT_RESULT" "$APP_RESULT"; do
              case "$result" in
                success|skipped) ;;
                *) exit 1 ;;
              esac
            done
            exit 0
          fi
          test "$AUDIT_RESULT" = success
          test "$APP_RESULT" = success
