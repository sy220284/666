# WorldForge 开发代理快速执行入口

> `AGENTS.md` 是仓库级完整且唯一的权威指令。本文件只保留高频执行规则；冲突、缺项或细节不明时，立即回到 `AGENTS.md` 和任务卡，不得自行补全规则。

## 1. 必读顺序

```text
AGENTS.md
→ docs/PROJECT_EXECUTION_ENTRY.md
→ docs/tasks/ACTIVE_TASK.json
→ docs/tasks/ACTIVE_TASK.md
→ 当前活动任务指向的任务卡
→ 任务卡列出的专项文档
→ 真实代码、测试、数据结构升级、功能约定和最近提交
```

`ACTIVE_TASK.json` 是授权模式、活动任务、分支、允许路径、禁止路径和验证命令的机器真源。不得在本文件中固化当前任务、当前阶段、任务数量或授权模式。

## 2. 最短可靠执行路径

```text
精准识别目标、非目标和适用范围
→ 核对任务、依赖、基线、分支和允许路径
→ 识别上游、下游及横向、纵向关联影响
→ 检查真实实现、测试、数据结构升级、功能约定和最近变更
→ 建立失败复现或验收测试
→ 完成最小完整纵向实现和必要界面接线
→ 验证主路径、边界路径、失败路径及关联回归
→ 复查功能、性能、体验、安全、数据和恢复
→ 同步功能约定、文档、追踪矩阵、任务状态和验证记录
→ 永久门禁、受控合并、主分支验证
→ 重新读取主分支后再声明完成
```

重复任务优先复用已经验证的行动路径。失败先定位原因，不得通过绕过门禁、缩小验证范围或伪造成功推进。

### 网络与依赖阻塞回退

- 依赖安装或工具下载失败时，先核对版本、软件源、代理、域名解析、连接加密、认证、缓存和网络出口，区分本地配置错误与外部服务故障。
- 确认当前环境无法恢复依赖获取后，不得反复无效重试、随意降级版本、改写锁文件或使用未经验证的第三方镜像拖延开发。
- GitHub Actions 可正常联网时，立即按 `packageManager`、锁文件、Node版本、操作系统和处理器架构生成版本锁定的离线工具链或完整依赖包，附带来源提交、锁文件摘要、版本清单和SHA-256校验，并保存为自动化产物；长期稳定包可另存为发行附件。
- 通过GitHub连接器下载自动化产物到当前环境，校验摘要和平台兼容性后解压到仓库外或 `.gitignore` 已排除目录，再真实运行版本、格式、类型、构建和测试命令验证可用性。
- `node_modules`、`.pnpm-store`、Electron、esbuild、Playwright等依赖和二进制不得提交到普通Git分支；仓库只保存可复现的永久工作流、脚本和说明。产物上传、下载或解压不能单独证明完成。
- 该回退只替代依赖获取路径，不得绕过锁文件、任务范围、永久门禁、受控合并或主分支验证；能直接在自动化环境完成的验证应继续执行，避免网络故障卡住开发进度。

### 新任务全量代码基线复核

- 执行新任务卡前，必须以最新真实 `main` 为基线，通读已完成的全量代码及其测试、功能约定、数据结构升级、程序内部通信、界面接线和相关提交，确认当前真实实现起点。
- 必须梳理既有功能、公共能力、数据与状态流、错误语义、恢复路径以及横向、纵向关联影响，禁止只看任务卡和局部文件后直接编码。
- 逐项核对任务卡假设与现有实现，重点排查重复实现、并行真源、接口分叉、数据断层、状态断裂、数据结构升级冲突、界面脱节和测试语义偏离。
- 发现任务卡与真实代码不一致时，先记录偏离、影响和调整方案，必要时先修正任务卡、验收标准和配套文档，再开始实现。
- 新任务必须接入现有主流程并复用既有能力；完成后执行关联回归，确认新旧功能连续、跨层闭环、既有功能无回归且后续任务能够继续衔接。
- 未完成全量代码基线复核，不得声明任务已正确启动或完成。

## 3. 整体协调与代码质量

所有修复和新增必须从整体配合与协调出发，考虑：

- 横向：相邻功能、共享组件、公共功能约定、通用状态、共用测试、性能上限和作者体验；
- 纵向：应用界面、安全连接层、桌面主程序、本地服务、数据访问层、SQLite、数据结构升级、程序内部通信、验证记录和交付链路；
- 修改前识别上游输入、下游消费者、数据生命周期、失败传播和恢复路径；
- 修改后执行关联回归，确认没有引入新的功能、数据、安全、性能、兼容性或体验问题；
- 功能实现必须同时满足正确性、性能上限和可用体验。

代码必须同时具备：

```text
规范性
+ 健壮性
+ 可读性
+ 可维护性
+ 可扩展性
+ 可测试性
+ 性能可控性
+ 可观测性
+ 安全性
```

禁止用过度设计替代最小完整实现，也不得以局部优化破坏整体架构、单一真源、恢复能力或后续任务。

## 4. 五项硬边界

1. 作品正文、数据库、设置、索引、日志、生成指令、AI能力验证、配置和备份必须仅保存在用户本机，不得接入云存储或云同步。
2. AI输出先成为建议稿（内部对象：`Candidate`），未经作者采用不能进入当前稿（内部对象：`Draft`）。
3. `project.sqlite` 是唯一作品数据真源。
4. 锁定保护、保存序号、内容校验、不可变历史版本、事务、作品与路径边界由代码保证。
5. AI只能提议，作者拥有最终裁决权。

任一边界失败，立即阻断。

## 5. 分支、实现与验证

- 仓库长期只使用两个正式分支：`work` 用于全部任务实施与集成，`main` 用于受控合并后的稳定主线。
- 禁止为任何任务、修复、验证、治理关闭或并行工作新增分支，包括 `work/<任务>`、`feat/*`、`fix/*`、`chore/*`、`policy/*`、`probe/*`、`stage/*` 和 `validate/*`。
- 所有任务必须从最新 `main` 同步到同一个 `work` 分支执行；同一时刻只允许 `work → main` 的一个正式合并请求和一个最终受检提交。
- 任务完成并通过永久门禁后，按仓库规范由 `work` 合并到 `main`；`Main Verification` 成功后，必须将 `work` 快进同步到最新 `main`，再开始下一项任务。
- 禁止使用辅助分支、任务专属分支或第二个纯证据分支；开发、测试、审查和文档并行只能通过工作区隔离与提交协调完成，最终统一落入 `work`。
- 两个及以上正式文件必须优先使用Git对象树形成单个原子提交。
- 不得修改 `ACTIVE_TASK.allowedPaths` 之外的文件，不得重构无关代码。
- 不得用待办标记、空实现、固定成功、硬编码演示数据、吞错、只写测试或临时运行环境结果冒充完成。
- 修复必须复验原问题；新增必须验证主路径、边界路径、失败路径和作者可操作闭环。
- 必须运行任务卡要求的专项检查；未经真实执行不得声称通过。
- 门禁前后工作树必须干净，关键文件写入后必须从真实合并请求受检提交重新读取。

## 6. 必须停止并重建的情况

- 写入错误仓库、分支或路径；
- 自动化生成的正式源码不在合并请求受检提交；
- 出现任务专属工作流、补丁目录或临时脚本压过正式实现；
- 不同门禁验证不同代码树；
- 任务、验证记录、主线来源或活动任务状态不一致；
- 数据库、数据结构升级、安全、作品边界、事务、恢复或关键性能上限失败。

## 7. 完成声明

只有以下条件全部成立，才能声明主线闭环：

- 受检提交的永久门禁真实通过；
- 受控合并已实际完成；
- 主分支验证已成功；
- 必要状态已进入 `main`；
- 已重新读取真实 `main` 和关键文件；
- 原始要求已逐项复核；
- 关联功能、性能、体验、安全和数据未出现新回归。

运行环境成功、合并请求可合并、自动化产物上传或补丁生成不能单独证明完成。

## 8. 正式中文名称与协作说明

- 正式中文名称以 `docs/product/AUTHOR_LANGUAGE_GLOSSARY.md` 为唯一业务语言真源。
- 应用界面、帮助、错误提示、任务卡、合并请求、提交说明、测试标题、代码注释、自动化步骤和验证记录必须优先使用正式中文名称。
- 指向具体代码时，中文业务名称在前，内部类型、命令或字段放在反引号内作为补充。
- 文件路径、分支名、代码标识、命令、框架、协议和官方技术名称可以保留英文，解释性文字必须使用中文。
- 新增业务概念必须先登记正式中文名称、内部标识、适用场景和禁止同义词。
- 不得将建议稿、当前稿、历史版本、AI连接、设定更新建议、作品检查等业务概念重新写回内部英文名。
- Git提交标题与说明、合并请求标题与描述，以及推送、同步、合并、关闭、回滚和诊断中的人工说明必须使用中文。
- `fix`、`feat`、`chore` 等英文类型前缀改为“修复”“功能”“维护”“文档”“测试”“重构”等中文表述。
- 受控合并使用合并请求标题生成主分支的压缩提交标题，因此标题必须准确、完整并使用中文。

推荐格式：

```text
修复(M8-04)：当前稿变化时阻止覆盖
功能(M8-04)：增加建议稿差异导航
体验(M8-04)：将AI连接改为预设向导
文档：统一作者语言与开发术语
```
