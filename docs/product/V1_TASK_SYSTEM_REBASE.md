# WorldForge V1.0任务体系重排基线

> 状态：Frozen Historical Baseline  
> 初始重排日期：2026-07-14  
> 整体收口日期：2026-07-25  
> 后续演进更新：2026-07-30  
> 适用：解释M4-04形成原因、需求吸收关系和历史实施顺序；当前任务数量与状态以`../tasks/TASK_INDEX.md`为准。

## 1. 历史重排基线

V1历史任务规格保留M0—M8编号和54份任务文件，用于需求、设计、测试和历史追踪。

2026-07-14至2026-07-25期间，独立执行体系曾收口为34张任务：

```text
M0-01—M0-07
M1-01—M1-09
M2-01—M2-04
M3-01—M3-10
M4-01—M4-04
```

当时M0—M3与M4-01—M4-03已经Verified；M4-04作为V1剩余功能唯一整体任务。原M4-05—M8-03共20份任务卡保留为详细需求来源，状态为`Removed（absorbed by M4-04）`。Removed只取消执行形式，不取消需求、非目标、合同、测试与完成条件。

本节记录当时决策，不再作为当前活动任务或任务数量真源。

## 2. 收口原因

逐张任务卡推进在已有代码规模下产生以下风险：

1. 同一用户功能被合同、数据库、Core、IPC和UI任务边界切断。
2. 后一张卡才发现前一张卡缺少必要字段、事件、类型守卫或恢复语义。
3. Prompt、TaskProtocol、Candidate、导入和恢复基础可能被重复封装。
4. 任务切换导致共享Schema、Migration、IPC入口和状态模型频繁变化。
5. 单卡完成易形成“后台已存在、用户不可用”或“UI已展示、权威链路未接”的假闭环。
6. 状态提取、校验、写作统计、备份等跨层能力需要统一数据生命周期和失败传播。
7. M7/M8后置体验与验收可能被迫返工早期局部实现。

因此，作者批准将当时剩余工作合并为M4-04，先读取全部要求与全量代码完成总规划，再连续实施。

## 3. M4-04历史范围

```text
已完成底座
├─ M0—M3
├─ M4-01 FTS
├─ M4-02 ConstraintPackage
└─ M4-03 Provider

M4-04整体范围
├─ Prompt、GenerationRun与模型支持档案
├─ 作者工作流与产品体验收口
├─ T0/T1、改写、融合、候选审阅和采用
├─ 状态提取、校验与人物弧光一致性
├─ 搜索替换、写作统计与网文节奏
├─ DOCX、多格式导出与三轨恢复
└─ C0—C7核心功能闭环
```

M4-04最终吸收原M4-05—M6-06并完成C0—C7；C8体验、硬化与自用交付随后由恢复为独立任务的M8-02承接。

## 4. 历史内部实施顺序

```text
1. 全量基线审计与整体规划
2. AI公共合同与运行底座
3. 作者体验与AI写作闭环
4. 状态提取、校验与连续性
5. 搜索、统计、导入与恢复
6. 完整体验、硬化与发布关闭
```

内部阶段只用于排序和复查，不是独立任务，不切换`ACTIVE_TASK`。

## 5. 历史单任务实施模式

```text
一个活动任务
→ 一个正式分支
→ 一个长期Draft PR
→ 多个可定位原子提交组
→ 每阶段代码审计与受影响回归
→ 全部完成后一次转Ready
→ 六项永久门禁通过后一次受控合并
→ 一次整体Verified关闭
```

M4-04正式分支为`work/m4-04-v1-integrated-delivery`。该任务已经Verified，不得重新激活或用于承接后续维护。

## 6. 纵向闭环与共享真源

每项用户功能必须按实际影响完成：

```text
Contracts
→ Domain（适用）
→ Migration / Repository（适用）
→ Core Use Case
→ Electron Main
→ Preload
→ Renderer
→ 自动化与人工验收
→ 文档、追踪与证据
```

以下公共能力只允许一套权威实现：

- Prompt Registry、Parser、Cleaner与Eval绑定。
- TaskProtocol、流式、取消、背压和任务快照。
- Candidate、Diff、ConflictSet、ApplyRecord和撤销。
- ImportPlan和导入提交协调器。
- RecoveryService、备份格式和恢复事务。
- 新手/专业模式、Appearance和Theme状态。

该原则继续适用于后续独立维护任务。

## 7. 后续演进

M4-04关闭后，任务体系按真实交付边界继续演进：

```text
34张历史收口任务
→ M8-02恢复为独立任务，完成C8、硬化和自用便携交付
→ M8-04独立完成作者体验与开发语言统一
→ 形成36张全部Verified任务
→ M8-05独立修复运行时竞态、错误语义与文档漂移
→ 当前共37张独立任务
```

原M7-01—M7-03、M8-01和M8-03由M8-02吸收；M8-04、M8-05均为后续新立项的独立维护任务，不属于M4-04历史收口范围。

## 8. 当前权威入口

当前执行不得从本历史文档推断，必须依次读取：

- `docs/tasks/ACTIVE_TASK.json`
- `docs/tasks/ACTIVE_TASK.md`
- `docs/tasks/TASK_INDEX.md`
- 当前活动任务卡
- `docs/PROJECT_EXECUTION_ENTRY.md`

## 9. 历史冻结与兼容扩展

- 已完成任务卡、历史Evidence和已发布Migration保持冻结。
- 既有能力发现缺陷时，由新的独立维护任务追加兼容合同、实现、回归测试和文档。
- 不通过改写历史目标或证据伪装新能力已完成。
- 不允许需要后续任务才能解释、恢复或迁移的数据进入Ready Head。
- 无AI基础写作产品门始终有效。
