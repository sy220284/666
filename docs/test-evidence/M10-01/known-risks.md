# M10-01 已知风险

- Renderer仍有18个React或DOM生命周期文件采用精确覆盖排除；本任务未扩大排除范围。章节状态策略、源码不变量、专项单元测试和Electron E2E共同提供替代证据。
- Task缺口恢复采用重复Snapshot收敛策略；极端持续高频缺口会增加Snapshot请求次数，但每个任务始终保持单一恢复循环，不并发读取。
- 性能测试改为测试文件串行执行，预算值、样本逻辑和失败断言保持不变；执行时间略有增加，用于避免同Runner并行重负载造成测量污染。
- pnpm 11.13.1已验证通过当前永久工作流；后续升级仍需独立依赖治理和完整质量矩阵。
