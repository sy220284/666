# M10-07 已知风险

1. Draft 阶段未执行单元、集成、Migration、覆盖率、构建与桌面 E2E；这些结果必须由 Ready Head 完整验证。
2. 恢复前自动留档可从历史版本列表读取，当前任务未增加独立的 archived Draft 管理工作台，符合任务非目标。
3. Safe Replace 的 stale 后置失败保存在原错误对象的非枚举诊断属性中，当前未单独写入持久化日志。
4. 统一序列化器只处理 JSON 兼容的持久化结构；`undefined` 仍不属于受支持输入，语义与迁移前一致。
