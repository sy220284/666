# M10-08 已知风险

1. Renderer TSX 首次进入真实 Coverage 分母后，覆盖率可能低于既有 75% 阈值。不得通过降低阈值或扩大排除范围维持表面数字；应依据 Ready 报告补充测试或逐项证明运行环境排除。
2. React Hooks 专项插件尚未加入锁文件。本任务未手工伪造依赖状态；后续需在可复现依赖治理任务中引入与 React 19、ESLint 10 兼容的插件。
3. 已发布 Migration 中的历史无条件数据回填按 app 0002、project 0029 截止版本冻结读取。新 Migration 仍必须使用 WHERE 或显式审查注解，历史文件不得反向改写。
4. 文件规模报告只用于人工观察，不会阻断合并。结构风险仍由循环依赖、跨层方向、Feature 边界和状态所有权门禁控制。
5. Toolchain Export 启动时遗留的临时分片提交仍存在于 work 历史，但最终 PR 树已完全删除相关文件，Squash 合并不会把二进制产物带入 main。
