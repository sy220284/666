# M11-07 验证总结

- 任务：`M11-07` 长篇智能底座收口
- 来源 PR：`#395`（`work → main`）
- 实现冻结提交：`946a6e83528b9d87929dc8bbcd914f04964b2102`
- 启动基线：`main/work/governance@023ae756103694082a59810fdb8fd3f249b5f3ab`

## 实现范围

1. 新增可重建的章节、卷、全书三级故事摘要，保存来源定稿、语义修订和新鲜度；章节定稿先提交，摘要重建失败不会回滚作者定稿。
2. 现有约束包继续作为唯一智能上下文权威，接入新鲜故事摘要和项目文风档案，并保留正式人物、设定、连续性资料的来源追踪与裁剪职责。
3. 新增长篇创作设置：预设、手动和正文学习文风档案，非阻断式文风偏离提醒，以及按任务、凭据、精确模型与生成指令版本选择首选/回退连接。
4. 日常智能生成入口收敛为“规划这一章 / 生成这一章 / 改写选中内容”，来源、目标字数、继续生成、候选数量和融合能力仍可从高级设置进入。
5. 新增 `Ctrl+K` 命令面板，复用既有全文搜索、结构、故事知识、原子导航和页面动作；支持能力过滤、输入法组合态、焦点圈闭、返回焦点与旧请求隔离。
6. 项目 Migration 升至 Schema 34；克隆不复制派生摘要，按当前正式正文重新生成。作者界面不暴露摘要哈希或内部语义修订。

## 本地验证

- `pnpm test:coverage`：373 个测试文件通过；1505 个测试通过，1 个设计性跳过；核心 TypeScript 与 Renderer TSX 双轨覆盖门均通过。
- `pnpm test:reliability`：8 个文件、10 个测试通过，1 个设计性跳过；3 个变异体全部被杀死。
- `pnpm test:eval`：3 个文件、9 个测试通过；关键召回覆盖率 100%，FTS 召回率 100%，关键语义漏检率 0，结论为不触发向量层。
- `pnpm typecheck`、`pnpm lint`、`pnpm format:check`、`pnpm build`、语言/工作区/边界/文档/治理/许可证检查全部通过。
- Linux 本地环境无 `DISPLAY`/Xvfb；Electron E2E 完成构建后以 `E2E_DISPLAY_UNAVAILABLE` 停止，完整桌面流程由 Ready Head 的 GitHub Actions 执行。

## 长篇规模、恢复与内存

- 300万字 / 600 章：项目打开 788.30ms，全量摘要 1015.61ms，增量摘要 69.36ms，全文搜索重建 2263.90ms，命令面板 51.06ms，上下文组装 108.11ms。
- 500万字 / 1000 章：项目打开 1219.40ms，全量摘要 1002.15ms，增量摘要 111.95ms，全文搜索重建 5762.24ms，命令面板 29.64ms，上下文组装 127.47ms；备份 1963.91ms，恢复 2461.80ms。
- 规模测试堆增长 7,741,792 bytes；500万字 SQLite 87,334,912 bytes，备份 87,375,872 bytes，全部预算通过。
- 强制 GC 稳定态探针独立复验通过：2450 次连续写作最终堆变化 -14,688 bytes；1100 次项目开关最终堆增长 475,832 bytes，均在强制预算内。
- 全仓性能并跑中该隔离探针曾因本地重负载达到 150 秒子进程时限；同一脚本独立运行通过，永久资格以 Ready Head 标准 runner 的 Performance/Quality 门为准。

## 跨卷连续性与安全边界

- 两卷定稿中的“铜铃 / 守灯人 / 钟楼旧约”可通过章节、卷、全书摘要进入现有约束包，并保存两份定稿来源；替换第一卷定稿后，三级摘要同步变为待更新并在增量重建后提升语义修订。
- 模型路由拒绝无凭据连接，按精确 Provider、模型、任务类型、Prompt ID 和 Prompt Version 判断适配程度；主连接不满足最低适配时才回退。
- IPC 使用严格契约，拒绝凭据字段、重复文风/任务路由、重复回退和主回退重叠；凭据不进入摘要、文风设置或 Renderer。

## 状态结论

M11-07 静态状态为 `IMPLEMENTED`，来源绑定为 PR #395。Controlled Merge 后只有 `main-verification` 与 `task-verification/M11-07` 在主线提交上同时成功，才形成最终有效 `VERIFIED` 事实。
