# 已知风险

- `provider_stub`只代表经过Schema校验的状态提取输入；真实模型质量由后续Prompt与Eval任务验证。
- EndingSnapshot是可重建派生数据；快照全部stale或缺失时会回退权威当前表，读取成本高于有效快照。
- 当前失效传播记录范围和变化类型，不自动改写后续正文；作者仍需依据提示复核受影响章节。
- M3-06最终Verified按M3连续实现与批次复验规则延期；M4激活前机器门要求M3全部Verified且延期账本清空。
