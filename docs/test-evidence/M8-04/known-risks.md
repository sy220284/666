# M8-04 已知限制

- M8-04是在既有三平台自用便携交付基线上的作者体验维护，不扩大到第三方分发、企业部署或应用商店发布。
- Ubuntu托管运行器的虚拟显示验证覆盖应用界面启动与自动化交互，但不等同于物理混合DPI、多屏插拔、真实中文输入法和实体读屏人工体验。
- Windows代码签名、macOS签名与公证、系统安装器以及安装、升级、自动更新和卸载生命周期继续遵循V1.0自用发布边界。
- 真实AI服务账号、线下模型质量与本地GPU模型矩阵不属于本次维护范围；无AI写作和确定性服务桩路径保持永久验证。
- 请求代次只保证应用界面忽略过期响应，不取消已经进入本地服务的只读查询；该行为不会产生权威数据写入。
- M8-04验证了跨作品和同一查询通道的旧响应防回写，但当时没有覆盖全文搜索、安全替换、作品词典与索引之间的交叉操作。
- 2026-07-30后续审计确认上述操作共享请求代次会导致词典保存或删除使在途搜索/替换持续等待；该缺陷由独立任务M8-05修复和验证，不改写M8-04历史Manifest。
- 关闭握手在应用界面无响应或校验失败时阻止静默退出，作者仍可选择操作系统级强制结束并依赖既有恢复机制。
