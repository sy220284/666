# WorldForge 安全测试用例

> 状态：Approved  
> 依据：`SECURITY.md`、`THREAT_MODEL.md`、五项ADR和P0硬保证。

## 1. Electron隔离

| ID      | 场景             | 步骤                                         | 预期                                       |
| ------- | ---------------- | -------------------------------------------- | ------------------------------------------ |
| SEC-001 | Renderer访问Node | 在Renderer尝试读取`process`、`require`、`fs` | 均不可用，应用不崩溃                       |
| SEC-002 | 原始IPC访问      | 尝试调用未暴露channel                        | 无通用接口，命令无法发送                   |
| SEC-003 | 不安全脚本       | 正式构建注入`eval`或内联脚本                 | CSP阻止并记录安全事件                      |
| SEC-004 | 应用内远程导航   | 点击或脚本跳转外部URL                        | 当前窗口不导航，允许的外链交系统浏览器     |
| SEC-005 | 新窗口           | 页面请求`window.open`                        | 被拦截或交系统浏览器，不创建高权限远程窗口 |

## 2. IPC契约

| ID      | 场景                 | 预期                                   |
| ------- | -------------------- | -------------------------------------- |
| SEC-006 | 未注册命令           | 返回输入/命令错误，不进入Core Use Case |
| SEC-007 | 多余字段             | 严格Schema拒绝                         |
| SEC-008 | 非法枚举和超长字符串 | 拒绝并返回稳定错误码                   |
| SEC-009 | 协议版本不匹配       | 只允许健康检查和安全退出               |
| SEC-010 | 重复requestId        | 写操作只执行一次，返回原结果           |
| SEC-011 | 伪造projectId        | Core拒绝，数据库无变化                 |
| SEC-012 | 伪造实体ID跨项目     | Core拒绝，源项目和目标项目均无变化     |

## 3. 路径与文件

| ID      | 场景               | 预期                             |
| ------- | ------------------ | -------------------------------- |
| SEC-013 | `../`路径穿越      | 规范化后发现越界并拒绝           |
| SEC-014 | 绝对路径绕过项目根 | 非用户明确选择目录时拒绝         |
| SEC-015 | 符号链接逃逸       | 真实路径不在允许根目录时拒绝     |
| SEC-016 | 项目移动到已有目录 | 显示冲突，不覆盖文件             |
| SEC-017 | 导出覆盖现有文件   | 明确确认或更换目标，使用临时文件 |
| SEC-018 | 恢复覆盖原项目     | 默认不允许，恢复到新目录         |

## 4. Draft、Candidate与锁定

| ID      | 场景                  | 预期                           |
| ------- | --------------------- | ------------------------------ |
| SEC-019 | AI流式输出            | Draft内容和Revision不变化      |
| SEC-020 | 未确认Candidate       | Draft不变化                    |
| SEC-021 | 旧Revision采用        | 返回冲突，不静默覆盖           |
| SEC-022 | 块Hash变化            | 目标块进入冲突集               |
| SEC-023 | 锁定块更新            | Editor和Core均拒绝             |
| SEC-024 | 锁定块删除/移动       | 操作拒绝或明确跳过             |
| SEC-025 | 批量替换命中锁定块    | 预览标记并默认跳过             |
| SEC-026 | Version修改           | Repository无更新路径或明确拒绝 |
| SEC-027 | partial Candidate定稿 | 拒绝，要求先确认或转完整稿     |

硬指标：锁定块破坏、未确认Candidate写入和Revision静默覆盖均为0。

## 5. 数据库与Migration

| ID      | 场景                        | 预期                                       |
| ------- | --------------------------- | ------------------------------------------ |
| SEC-028 | 写事务中断                  | 当前事务回滚，数据库可重新打开             |
| SEC-029 | Migration中断               | 旧Schema可恢复或继续升级，不处于半迁移状态 |
| SEC-030 | Migration checksum变化      | 停止写入并进入诊断                         |
| SEC-031 | 高版本项目                  | 只读打开，不尝试降级写入                   |
| SEC-032 | 完整性检查失败              | 停止写入，提供恢复和导出                   |
| SEC-033 | 并发自动保存和Candidate保存 | 无`SQLITE_BUSY`外泄和丢写                  |
| SEC-034 | FTS损坏                     | 业务表不受影响，可重建索引                 |

## 6. 凭据与Provider

| ID      | 场景                            | 预期                        |
| ------- | ------------------------------- | --------------------------- |
| SEC-035 | 保存Provider密钥                | 数据库仅出现`credentialRef` |
| SEC-036 | Renderer查询配置                | 返回元数据，不返回密钥      |
| SEC-037 | Provider认证失败                | 错误不回显认证头和密钥      |
| SEC-038 | 日志扫描                        | 不出现测试密钥              |
| SEC-039 | 重定向到其他主机                | 默认拒绝或要求明确协议允许  |
| SEC-040 | `file://`或内部协议Provider地址 | 拒绝                        |
| SEC-041 | 局域网端点                      | UI明确提示内容离开当前设备  |

## 7. DOCX与导入

| ID      | 场景            | 预期                           |
| ------- | --------------- | ------------------------------ |
| SEC-042 | `.docm`宏文档   | 拒绝导入                       |
| SEC-043 | ZIP路径穿越条目 | 拒绝并清理临时目录             |
| SEC-044 | 高压缩比文档    | 超过阈值停止解析               |
| SEC-045 | 文件数过多      | 超过阈值停止解析               |
| SEC-046 | OLE嵌入对象     | 忽略，不执行或提取为可执行内容 |
| SEC-047 | 外部图片/链接   | 不自动请求外部资源             |
| SEC-048 | 解析失败        | project.sqlite无新增业务数据   |
| SEC-049 | 取消预览        | 临时文件清理，项目不变         |

## 8. 日志与诊断

| ID      | 场景             | 预期                           |
| ------- | ---------------- | ------------------------------ |
| SEC-050 | 正常写作日志     | 不包含正文样本                 |
| SEC-051 | AI失败日志       | 不包含完整Prompt和模型原始响应 |
| SEC-052 | 数据库异常日志   | 不包含业务字段值和完整本地路径 |
| SEC-053 | 诊断包默认导出   | 不含项目库、备份、正文和凭据   |
| SEC-054 | 用户授权最小摘录 | 生成前可预览和删除，字符数受限 |

## 9. 备份与恢复

| ID      | 场景                   | 预期                            |
| ------- | ---------------------- | ------------------------------- |
| SEC-055 | 写入期间日常备份       | 使用Online Backup，恢复内容一致 |
| SEC-056 | 备份Hash失败           | 不标记为verified                |
| SEC-057 | 删除最后一份已验证备份 | 拒绝                            |
| SEC-058 | 恢复目标冲突           | 不覆盖，要求新目录              |
| SEC-059 | 恢复中断               | 原项目不变，失败副本可安全清理  |
| SEC-060 | 迁移前恢复点           | Migration开始前已完成并验证     |

## 10. 自动化与证据

- 单元/集成测试覆盖SEC-006—041和SEC-050—060。
- Playwright覆盖SEC-001—005、017、018和关键UI提示。
- 异常DOCX Fixture覆盖SEC-042—049。
- 每次Electron、Preload、路径、Migration、Candidate采用和备份变更必须运行相关安全用例。
- 结果保存到`docs/test-evidence/<TASK-ID>/security/`。

任一Critical或High用例失败时禁止合并。

## M1-08 恢复与只读安全基线

- SQLite在线备份必须通过`integrity_check`、`foreign_key_check`和SHA-256验证后才登记为BackupRecord。
- 空间不足、备份损坏、恢复中断和目标冲突不得留下可选中的未验证恢复点或部分项目。
- 数据库外键或完整性损坏时项目只能只读打开，全部写命令失败；外部已验证恢复点仍可恢复到新目录。
- 恢复副本必须重新生成项目ID并注册为独立最近项目，源工作区与源数据库不被覆盖。
- 只读状态下允许将仍可读取的不可变Version导出为UTF-8 TXT，目标文件存在时不得覆盖。

## M2-04 结构操作与永久删除基线

- Renderer不得提交`backupId`、影响数量、权威排序键或提交Revision；Main在可信来源检查后使用strict Schema再转发。
- 拆章、合章和跨章移动在创建恢复点前执行读预检，恢复点后在事务内再校验`planHash`、Revision、Hash与LockGuard。
- 锁定块、过期预览、非源Draft块、目标logicalBlockId冲突和事务中断均保持原Draft、Revision、PatchLog和结构不变。
- 永久删除在Version/Candidate引用存在、`planHash`过期、完整标题不匹配或用户取消时拒绝；成功路径必须返回已验证`backupId`并通过`foreign_key_check`。
